# Contributing

1. Fork → create a feature branch
2. Keep PRs focused and add tests/demos in `examples/` if UI-visible
3. Follow project style (no external CSS frameworks)

## Dev quickstart
This is a single-file library — open `examples/*.html` locally in a browser to test.
